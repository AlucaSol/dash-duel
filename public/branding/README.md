Drop a `logo.png` in this folder to replace the main-menu wordmark with your
own artwork. It's shown at up to 480×160 (aspect ratio preserved); transparent
PNG recommended.

If no `logo.png` is present, the menu falls back to the built-in gradient
text logo automatically — nothing else needs to change.
